- user places gates (drawn as vector images) and connects them by wiring using non-tile lines/curves
- some kind of projects version control, like git
- let users create their projects without login, save them locally and suggest them to log in to save them